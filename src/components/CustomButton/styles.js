const styles = (theme) => ({
    root: {
        fontSize: 30,
        borderRadius: 14,
        padding: 7,
        background: "#585252",
    },
    actionButton: {
        background: "#f19a37",
    },
});

export default styles;
